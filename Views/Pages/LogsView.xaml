<UserControl x:Class="TestManager.Views.Pages.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Журнал тестирования" FontSize="20" FontWeight="Bold" Foreground="#FF091B41" Margin="0,0,0,10"/>

        <Border Grid.Row="1" BorderBrush="#FF091B41" BorderThickness="1" Background="#FFF5F6FA">
            <ListBox ItemsSource="{Binding Logs}" Background="Transparent" BorderThickness="0" 
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Text}" Foreground="{Binding Color}" 
                                   TextWrapping="Wrap" FontFamily="Consolas" FontSize="14" Margin="0,2"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>