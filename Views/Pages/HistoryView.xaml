<UserControl x:Class="TestManager.Views.Pages.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:TestManager.ViewModels">

    <!--<UserControl.DataContext>
        <vm:HistoryViewModel />
    </UserControl.DataContext>
-->
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="История тестирования (БД)" FontSize="20" FontWeight="Bold" Foreground="#FF091B41" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal">
                <Button Content="Обновить данные" Command="{Binding RefreshCommand}" Background="#FFEB075E" Foreground="White" Height="25" Padding="15,0" BorderThickness="0" Margin="0,0,15,0"/>
                <TextBlock Text="Всего записей: " VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#FF091B41"/>
                <TextBlock Text="{Binding TestHistory.Count}" VerticalAlignment="Center" Foreground="#FF091B41"/>
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding TestHistory}" AutoGenerateColumns="False" IsReadOnly="True" BorderBrush="#FF091B41" BorderThickness="1" HeadersVisibility="Column" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Время" Binding="{Binding Timestamp}" Width="130"/>
                <DataGridTextColumn Header="Источник" Binding="{Binding Source}" Width="90"/>
                <DataGridTextColumn Header="Команда" Binding="{Binding Command}" Width="200"/>
                <DataGridTextColumn Header="Ожидаемое" Binding="{Binding TargetValue}" Width="100"/>

                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="5,0"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Успех">
                                    <Setter Property="Foreground" Value="#43A047"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Ошибка">
                                    <Setter Property="Foreground" Value="#E53935"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Таймаут">
                                    <Setter Property="Foreground" Value="#FB8C00"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Вывод" Binding="{Binding Output}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>