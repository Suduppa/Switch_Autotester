<UserControl x:Class="TestManager.Views.Pages.ResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Excel Отчет тестирования" FontSize="20" FontWeight="Bold" Foreground="#FF091B41" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="Обновить отчет" Command="{Binding LoadReportCommand}" Background="#FFEB075E" Foreground="White" Padding="15,5" BorderThickness="0"/>
        </StackPanel>

        <TabControl Grid.Row="1" ItemsSource="{Binding Sheets}">

            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding TableName}" FontWeight="SemiBold" Foreground="#FF091B41"/>
                </DataTemplate>
            </TabControl.ItemTemplate>

            <TabControl.ContentTemplate>
                <DataTemplate>
                    <DataGrid ItemsSource="{Binding DefaultView}" 
                              AutoGenerateColumns="True" 
                              IsReadOnly="True" 
                              HeadersVisibility="Column"
                              Background="White"
                              BorderBrush="#FFC2C5DC"
                              HorizontalGridLinesBrush="#FFE0E3F3"
                              VerticalGridLinesBrush="#FFE0E3F3"
                              RowBackground="#FFF5F6FA"
                              AlternatingRowBackground="White"/>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

    </Grid>
</UserControl>